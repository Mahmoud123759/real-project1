apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: production
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  backend_url: "http://backend-active.production.svc.cluster.local"   # internal ALB or service name