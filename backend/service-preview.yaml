apiVersion: v1
kind: Service
metadata:
  name: backend-preview
  namespace: production
spec:
  selector:
    app: backend
  ports:
    - name: http          # <-- added
      port: 80
      targetPort: 8080